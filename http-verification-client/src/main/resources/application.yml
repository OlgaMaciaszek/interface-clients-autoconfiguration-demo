server:
  port: 8080
spring:
  interface-clients:
    http:
      client-groups:
        default:
          base-url: http://localhost:9999
        verificationClient:
          base-url: http://localhost:8081
  application:
    name: http-verification-client
